node_modules
dist
*.log
*.tgz
.DS_Store
.env
.env.local
coverage

# We use pnpm. If someone accidentally runs `npm install`, ignore the
# lockfile it drops so we don't end up with two competing lockfiles.
package-lock.json
yarn.lock
